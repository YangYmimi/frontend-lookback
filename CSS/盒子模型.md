### 盒子模型

CSS 中可以通过设置 `box-sizing` 改变盒子模型的定义

* `content-box`: W3C标准盒子模型。盒子的 `width = contentWidth` 和 `height` 不包括 `border` 和 `padding` 和 `margin`

* `border-box`: IE盒子模型 `width = padding + border + content` 不包括 `margin`